# v004・r001 連結ウィンドウ改善の実装方針

作成日: 2026-09-08  
状態: 主要4段階とレビュー指摘3点の修正を検証し、現行機能範囲でFIX（2026-09-09）。
対象: v004の工程予測と、r001の連結ウィンドウ評価・条件選択。

## 1. 目的と結論

改善は実装可能であり、次の順で進める。

1. **本来ある許容窓を、誤許容を抑えながら正確に推定する。**
2. **より広い許容窓を持つ中心条件を探索する。**
3. **複数の操作条件が同時に変動した場合の成立範囲を検証する。**

現在の35.9%という結果は主に1の課題である。2の「窓幅を目的にした最適化」とは区別する。
推定が甘い場所を広い窓と誤認して選ばないよう、1の検証が成立してから2を有効化する。
物理的な限界自体をソフトウェアで広げるのではなく、見落としていた範囲の回収と、
許容変動幅の大きい運転条件の発見を目指す。

## 2. 現状と根拠

### 2.1 改善着手前に実装済みだった処理

- v004は操作条件と流入状態から各出力の平均・標準偏差・支持度を返す。
- r001は上流の予測平均を下流へ接続する。
- 工程内制約、接続先入力範囲、最終仕様に対し、平均±`confidence_z × std`で余裕を評価する。
- 支持度閾値を加えた余裕を使って、固定候補から中心条件を選択できる。
- 選択した中心の周囲で操作変数を一つずつ走査し、中心を含む連続した合格区間を出力する。
- **窓幅そのものを比較して中心を選ぶ最適化は、現状の選択処理にはない。**

主な実装参照:

- [v004/src/stage_bundle.py](v004/src/stage_bundle.py)
- [r001/src/cli.py](r001/src/cli.py): `_run_connected_window`、`_connected_window_profiles`、`run_trial`
- [multistage_validation/connected_window_validation.py](multistage_validation/connected_window_validation.py)

### 2.2 確認済みの検証結果

対象は塗工→乾燥→硬化の完全観測・決定論oracleと、
`trial_validated_individual_chain_r001`の保存済みモデル・中心条件。

| 設定 | 9断面の平均窓幅比 | 危険側へのはみ出し |
|---|---:|---|
| 現行 `confidence_z=1.645` | 35.9114% | 検証グリッドでは0 |
| 比較用 `confidence_z=0` | 89.7759% | 塗工ギャップの下限で発生 |

現行設定の35.9%と9軸のoracle境界は、今回のレビュー中に再計算して一致を確認した。
係数0の比較は、同じモデル・同じ中心を固定してメモリ上で設定だけ変更した追加診断であり、
既存設定・結果ファイルは変更していない。係数0の詳細結果は既存JSONには保存されていない。
塗工ギャップ207.75 µmを許容する一方、同点のoracleは最終仕様NG、品質余裕約−0.06018であった。

現行設定では9断面すべてで`process_window`と`trusted_window`が一致している。
この例の窓幅縮小は支持度閾値だけでは説明できず、閾値の緩和だけでは改善しない。

根拠:

- [連結窓の保存済み検証結果](multistage_validation/results/connected_window_validation.json)
- [個別工程の閉ループ検証](multistage_validation/results/stage_closed_loop_final_validation.json)
- [工程接続後の診断検証](multistage_validation/results/r001_diagnostic_validation.json)
- [工程入力ばらつきの確率評価](multistage_validation/results/validated_individual_chain_probability_calibration.json)

### 2.3 結果の解釈上の制限

- 35.9%は1中心・9個の一変数断面についての「推定幅÷oracle幅」の単純平均である。
  9次元領域の体積被覆率、条件全体の再現率、実機での許容率ではない。
- oracleの窓も有限範囲・有限グリッドで評価した区間であり、連続空間の厳密解ではない。
- 一変数断面の直積を、同時変更してよい箱として解釈してはいけない。
- 現行の検証`pass`は主に中心の合格と境界はみ出しの許容値で決まり、窓幅の下限を要求しない。
- v004の標準偏差は工程ばらつき・測定誤差などを分離していない。`1.645`は校正済みの
  工程全体信頼水準を意味しない。
- 上流標準偏差は接続範囲のバッファに使われるが、その分布全体を下流へ伝播していない。
  一律バッファが保守的であることは、全条件で安全が保証されることを意味しない。

## 3. 設計原則と責務

| 対象 | 担当する改善 |
|---|---|
| v004 | 工程ごとの残差評価、区間予測用情報、適用範囲・データ来歴の保存 |
| r001 | 工程接続後の誤差評価、連結窓の計算、中心条件の比較、同時変動評価 |
| multistage_validation | oracle比較、独立評価、誤許容と窓回収率の計測 |

- 既存v003互換経路と既存bundle読込を維持し、新方式は明示設定で有効化する。
- 制約・適用範囲・支持域外判定を、窓幅を広げるために黙って緩めない。
- 学習、バッファ調整、候補選択、最終評価のデータ用途を区別する。
- 決定論の許容窓、モデル誤差を考慮した推定窓、工程ばらつきに対する良品率を別指標にする。
- 新方式が必要とする情報のないbundleでは、従来方式へ明示的に戻すか未評価とする。
  不足を標準偏差0で補わない。
- 従来結果・設定・モデルを保持し、新結果を別名で保存できるようにする。

## 4. 第1段階: 窓の縮小原因を分解して見えるようにする

### 変更内容

各走査点で、次の判定を並べて記録する。

1. 予測平均だけでの工程制約・接続範囲・最終仕様判定。
2. 現行標準偏差バッファを加えた判定。
3. 支持度制限も加えた判定。
4. 検証時のみ、同一入力に対するoracle判定。

境界ごとに、律速となった制約、予測平均、標準偏差、バッファ量、支持度を保存する。
バッファとモデル平均誤差の影響を切り分け、どの工程・出力を改善すべきか特定する。

### 境界計算

- 粗い全域走査で中心を含む合格区間と他の合格区間を識別する。
- 合否が変わる区間を追加走査し、指定した操作量の許容誤差まで境界を絞る。
- 離れた合格区間を一つの区間に連結しない。
- グリッド間の狭いNG領域を見落とす可能性を残し、走査解像度と評価点数を記録する。
- 工程・最終仕様上の幅と、支持度制限後の幅を別々に報告する。

この段階は診断と測定の改善であり、選択方式は変えない。

## 5. 第2段階: バッファを予測誤差に合わせて調整する

### 5.1 データを分離する

少なくとも次を分ける。

- 学習用: 工程モデルの学習。
- 調整用: 予測残差の評価、バッファ方式・係数の選択。
- 最終評価用: 採否判定。方式選択・係数調整には利用しない。

同一製品・反復・ロットの情報が分割をまたがないようにする。
oracle試験ではseedに加えて流入状態・境界シナリオを分離する。
追加学習や方式変更のたびに同じ評価集合へ過適合しないよう、新しい確認集合も用意する。

### 5.2 区間誤差を工程・出力・方向別に評価する

- 上限制約では過小予測、下限制約では過大予測が誤許容につながる。
  このため片側残差の分位点を使う方法を候補とする。
- 現行標準偏差で正規化した残差と、出力の実単位による残差を比較する。
  標準偏差が小さい場合の除算には下限を設ける。
- 必要な標本数があれば、支持度や流入状態の領域ごとの調整を検討する。
  細分化で標本不足になる場合は、より広い区分へ戻して不足を表示する。
- 各出力の被覆率が高くても全制約同時の被覆率は保証されない。
  制約間の最大残差スコアによる同時評価なども比較する。

残差分位点やconformal方式を使う場合も、交換可能性などの前提を明示する。
候補を適応的に選ぶ探索、外挿、ドリフト下へ無条件の保証を拡張しない。

### 5.3 接続後の誤差も評価する

工程単体に真の流入値を与えた残差だけでは、上流誤差の影響を捉えられない。
上流から予測平均を順に接続した場合の中間状態・最終出力残差も測定する。

初期案は、固定したbundleの組み合わせに対して接続後の残差バッファを評価する。
工程単体バッファと接続後バッファを無条件に加算し、誤差を二重計上しない。
各バッファが何をカバーするかを定義した上で採用方式を比較する。
bundleが更新された場合は、その接続後調整結果を自動的に有効扱いしない。

工程ばらつき・モデル不確実性・測定誤差・出力相関を分離した同時サンプリング契約は
後続拡張とし、この段階で校正済み製品良品率を名乗らない。

### 5.4 境界付近の追加学習

モデル平均の誤差が原因の場合はバッファ調整だけでは改善しない。
ボトルネック制約の境界付近で、支持度が低い点やモデル間の判定差が大きい点を追加学習候補にする。
まずoracleデータで有効性を比較する。実機向けの自動実験実行はこの計画の範囲外とする。

## 6. 第3段階: 広い窓を持つ中心条件を探索する

### 6.1 最適化対象

各操作変数iに対し、中心からの下側・上側許容幅を`h_i^-`、`h_i^+`とする。
工程上必要な変動許容量を`d_i > 0`として、初期の目的指標を次のようにする。

```text
対称許容余裕 rho = min_i( min(h_i^-, h_i^+) / d_i )
```

これは、どの操作変数にも必要変動幅の何倍の余裕があるかを表す**断面上の指標**であり、
同時変動する箱の成立を意味しない。片側変動が本来の運用なら、上下で異なる必要幅を設定する。

`d_i`未設定時にモデル入力範囲で正規化する方法も比較用に使えるが、
その値を現場の変動耐性として表示しない。

### 6.2 選択手順

1. 必須制約、接続範囲、支持度条件を満たす候補を抽出する。
2. 従来余裕と条件空間の多様性から、窓評価対象の候補を選ぶ。
3. 各候補の周囲を走査し、対称許容余裕と断面幅を評価する。
4. 対称許容余裕を優先し、同率時は仕様余裕、必要なら明示された生産性指標で順位付けする。
5. 上位候補について、細かい境界計算と独立したoracle評価を行う。

評価対象を従来余裕の上位だけに限定すると、広い窓を持つ候補を落とす可能性がある。
多様性を確保した探索と、比較可能な小規模問題での全候補評価を併用する。
上位候補のみの評価結果を大域最適と表示しない。

### 6.3 計算量

全19,683候補について9軸×241点を個別に評価すると、約4,270万点の工程接続評価になる。
初期実装は候補絞り込み、粗密走査、バッチ予測、再利用可能な上流計算のキャッシュを使う。
評価予算と候補絞り込み方法を結果に残す。

## 7. 第4段階: 複数変数の同時変更を評価する

- 律速制約に関係する変数の二変数断面を評価する。
- 中心の周囲に候補領域を設定し、内部点・境界面・頂点を評価する。
- 頂点の合格だけでは非線形領域の内部合格を保証しないため、内部・面の追加探索を行う。
- 実際の工程入力変動分布が定義されている場合は、独立シナリオによる良品率も別途計測する。
- 幅の積やサンプル合格率を、そのまま厳密な9次元許容体積と呼ばない。

出力では「一変数断面」「同時変更のサンプル検証領域」「工程入力変動に対する推定良品率」を区別する。
有限サンプリングだけの場合は、検証点数・分布・未検証領域を明記する。

## 8. 検証指標と受け入れ条件

### 8.1 窓推定の評価

推定区間L、oracle区間Oについて以下を記録する。

| 指標 | 定義・目的 |
|---|---|
| 窓幅比 | `width(L) / width(O)`。既存35.9%と比較する指標 |
| 真の窓の回収率 | `width(L ∩ O) / width(O)`。はみ出しても改善扱いしない |
| 誤許容幅率 | `width(L \ O) / width(L)` |
| 危険側境界誤差 | oracle境界を外へ超えた距離。実単位と正規化値の両方 |
| 空窓率 | oracle窓があるのに推定窓がない割合 |
| 判定精度 | 真陽性・偽陽性・偽陰性・真陰性を数と率で報告 |
| 評価不能率 | 支持域外・接続不可などを除外せず、全対象中の割合で報告 |

空区間、幅0の区間、oracle中心NGの場合の集計規則を固定し、都合の悪い断面を平均から落とさない。
平均に加えて軸別、中心別、最小値、分位点を示す。

### 8.2 評価範囲

- 既存1中心を固定した再現試験。
- 学習seed、原料状態、支持度、境界への距離を変えた複数中心の試験。
- 一変数・二変数・複数変数同時変更の試験。
- 原料・設備ドリフトや適用範囲外を含む拒否・未評価試験。
- 同じ制約、モデル、候補集合、評価予算での従来方式との比較。

### 8.3 段階別の採否

1. **診断基盤:** 現行35.9114%を再現し、縮小を支配する制約を特定できる。
2. **推定改善:** 固定した独立評価集合で、危険側の誤許容基準を満たしつつ回収率が改善する。
3. **中心最適化:** 同じ評価予算で従来選択よりoracle上の対称許容余裕が改善する。
   一部条件で悪化する場合も全件報告する。
4. **同時変動評価:** 断面幅だけの改善と区別し、複数変数の評価でも改善を確認する。

初期研究目標として、既存中心の回収率を35.9%から60%以上にすることを置いてよい。
ただし60%は未検証の目標値であり、実現見込みや合格保証ではない。
危険側の許容誤差は先に操作量の単位で決め、走査を粗くして合格させない。
oracle試験では評価点上の誤許容0を第一目標とするが、それを連続空間・実機の安全保証に読み替えない。
実用版の誤許容率・最低回収率・必要な標本数は、対象工程の要求に合わせて評価前に固定する。

## 9. 想定する変更箇所・成果物

以下は候補名であり、既存ファイル形式を変更済みという意味ではない。

| 箇所 | 変更案 |
|---|---|
| `v004/src/stage_bundle.py` | 残差区間情報、方式版、調整データ来歴、適用範囲を保存・再読込 |
| v004の残差評価用モジュール（追加） | 学習から独立したデータの評価、出力別・片側誤差集計 |
| `r001/src/cli.py` | 新方式の設定・能力検査、既存方式の維持、選択指標の記録 |
| r001の窓評価用モジュール（追加） | 境界診断、粗密走査、候補中心の窓評価、同時変更評価 |
| `multistage_validation/connected_window_validation.py` | 複数中心・回収率・誤許容・空窓の評価 |
| r001・v004のテスト | bundle互換、保存再読込、片側制約、分離区間、空窓、非有限値、更新時無効化 |

設定候補は、区間推定方式、残差情報の参照先、選択指標、必要変動幅、境界許容誤差、
評価予算、同時変更評価方法とする。既存`confidence_z`の意味は変更しない。

追加成果物の候補:

- 境界診断JSON: 境界制約、バッファ、支持度、解像度。
- 候補比較CSV: 中心条件、断面余裕、同時変更評価、仕様余裕、評価点数。
- 検証JSON: 条件別の誤許容・回収率、データ分割、seed、モデルと設定のハッシュ。
- 人向け評価レポート: 何が改善し、どこが未評価か。

## 10. 実装順と今回の対象外

推奨する作業単位は次のとおり。

1. 現行結果の固定と境界診断の追加。
2. 残差調整用・最終評価用データの準備。
3. 区間予測方式の比較と、必要箇所の追加学習。
4. 複数中心での誤許容・回収率検証。
5. 窓幅を目的にした中心選択の追加。
6. 同時変更の検証と利用範囲の明文化。

実機の自動運転、実験の自動実行、物理設備の変更、分岐・合流工程への拡張、
潜在原因の同定、校正済み良品確率の全面実装は今回の改善範囲に含めない。

## 11. 実装結果（2026-09-09）

本方針をレビューし、制約を黙って緩和せず、データ用途を分離し、一変数断面と同時変更を
区別する設計は妥当と判断した。初期実装では、5.3で許容した「固定bundle組み合わせに対する
接続後残差校正」を採用し、実世界校正は対象外とした。

- 第1段階: 平均のみ／誤差バッファ込み／支持度込みの判定、分離区間、境界律速情報を実装した。
- 第2段階: 学習とは別に校正seed 20260921、方式選択seed 20260922を各4,096点用意した。
  工程・出力・方向別の実単位残差と標準化残差を比較し、偽陽性0の選択集合で真陽性最大となる
  標準化片側残差方式（片側`alpha=0.15`）を選んだ。校正はbundle SHA-256不一致時に無効化する。
- 第3段階: 215適格候補のうち、多様性を含めた96候補を評価する予算付き窓中心選択を実装した。
  予測`rho`は従来中心0.75から2.5、独立oracle `rho`は1.9167から2.7917へ改善した。
- 第4段階: 9変数同時変更の乱数内部点4,096点、全512頂点、律速3組の二変数断面を実装した。
  `rho`箱の独立oracle照合はTP 3,815、FP 0、FN 88、TN 193だった。

一変数断面の独立oracle評価は、平均窓回収率80.7187%、危険側はみ出し0、TP 1,260、FP 0、
FN 223、TN 691、空窓0、評価不能0となり、初期研究目標60%を超えた。ただしこれは固定した
外部原料状態、有限候補・有限グリッドの結果であり、実機安全保証や大域最適性を意味しない。
また上位8候補のoracle評価では、予測7位が最大oracle `rho=3.3333`だったため、学習モデルによる
中心順位にはまだ改善余地がある。

実装・結果:

- [r001実装](r001/src/cli.py)
- [連結残差校正](multistage_validation/calibrate_connected_window.py)
- [一変数窓のoracle検証](multistage_validation/results/connected_window_validation.json)
- [中心選択・同時変更のoracle検証](multistage_validation/results/window_center_joint_validation.json)
- [実trial要約](r001/trials/trial_validated_individual_chain_r001/output/run_summary.json)

残る拡張は、操作単位の許容誤差まで絞る境界二分探索、外部原料状態・bundle seedを変えた反復、
境界点の自動追加学習、v004 bundle単体で再利用できる残差sidecarである。現実設備との校正、
校正済みend-to-end良品確率、実験の自動実行は当初方針どおり今回の範囲外とする。

## 12. レビュー修正と版固定（2026-09-09）

校正の適用条件検査、区間推定方式の明示分岐、誤許容・空窓・回収率を含む検証合否を修正した。
旧校正・出力は`multistage_validation/results/pre_fix_20260909/`に保存した。
現行標準化残差方式のoffsetsは再生成前後で一致し、窓幅・中心・同時変更の結果も維持された。
第11節の評価不能0は旧実装の固定値であり、今回の修正後は接続評価不能98点と計数される。
これらは合格として扱わず混同行列にも含めている。
実単位方式も比較対象に加えたが、運用方式の自動変更は行わない。
再検証結果と版固定の範囲は[FIX記録](V004_R001_FIX_RECORD.md)にまとめた。
