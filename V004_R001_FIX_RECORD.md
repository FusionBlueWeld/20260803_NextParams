# v004・r001 FIX記録

日付: 2026-09-09  
判定: **現行機能範囲でFIX**。今回の確認範囲で、版固定を妨げる致命的な実装不具合は検出していない。

## 固定する範囲

- v004: v003互換CLI、流入状態を固定した単工程探索、平均・予測標準偏差・支持度の出力、stage bundle。
- r001: 直列の平均予測接続、連結窓の推定、予算付き窓中心選択、有限サンプルによる同時変更評価。
- NG診断は完全観測・期待レシピ既知の実験的機能、工程入力ばらつきの良品率は未校正推定の位置付けを維持する。

これは現行版の機能固定であり、実機での安全保証・校正済み良品確率・大域最適性を宣言するものではない。
既存のv004モデル学習ロジックは変更していない。未実装の拡張は後続版で扱う。

## 修正した3点

1. **校正の適用条件を検査する。** 原料状態、工程順、接続関係、予測器ハッシュ、manifestハッシュ、
   最終仕様、工程内制約の適用方針、支持度閾値が異なる場合は拒否する。
   原料状態を評価配列だけで変更した場合も拒否する。不足・非有限・上下逆転の残差情報を拒否する。
2. **区間推定方式を設定どおり切り替える。** 実単位残差と標準化残差の分岐を共通化し、工程内制約・
   最終仕様・接続範囲に同じ方式を適用する。情報不足時に従来バッファへ黙って戻さない。
   校正器は両方式を比較するが、運用方式は明示設定を維持する。
3. **誤許容を含む検証結果で合否を決める。** 中心の予測・oracle合格、全軸評価、誤許容0、空窓0、
   平均グリッド回収率60%以上、境界はみ出し上限をすべて要求する。
   中心から離れた許容区間の誤許容でも不合格にする。評価不能件数の0固定を廃止した。

校正処理のNumPyマスクが元配列を変更していた点も修正した。全工程で支持された同一標本集合を
各alphaで使い、繰返しや工程順に依存しない。運用中の標準化方式の残差値は旧結果と完全一致した。

## 互換性・移行

残差校正ファイルはschema **1.1**。適用契約を持たないschema 1.0は、再生成を求めて拒否する。
stage bundleのschema 1.0と従来の`std_multiplier`方式は維持する。
対象trialの残差校正は再生成済み。旧校正と出力は
[修正前の保存先](multistage_validation/results/pre_fix_20260909/)に保持している。

両方式比較では、調整集合上の実単位方式alpha=0.20がTP 48・FP 0だった。
運用方式を自動変更せず、設定済みの標準化方式alpha=0.15（TP 35・FP 0）を維持した。
調整集合だけで優れた方式へ置き換えず、方式変更は別途独立検証を伴う変更として扱う。

## 再検証

| 確認 | 結果 |
|---|---|
| r001自動テスト | 10件合格 |
| v004自動テスト | 33件合格 |
| multistage_validation自動テスト | 29件合格 |
| 実CLI `--prepare` / `--run` | 成功、19,683候補、連結適格215候補 |
| 原料粘度1.6→2.0の変更 | 旧校正の使用を拒否 |
| 区間方式切替 | 同じ両方式入り校正データでも、指定方式により合否が変わることを確認 |
| 離れた区間に誤許容1点を追加した再現ケース | 修正後は`fail` |
| 選択中心の一変数窓 | 修正後の合否条件ですべて合格 |
| 窓中心・同時変更oracle検証 | 合格 |

一変数窓の平均幅比は**80.7187%**、平均グリッド回収率は**80.9569%**。
2,174走査点のTP 1,260・FP 0・FN 223・TN 691を維持した。
接続評価不能は98点で、合格から除外して混同行列に含めている。旧表示の0件は固定値だった。
幅比とグリッド回収率は定義が異なる。旧35.9%との比較は中心条件も異なり、推定方式だけの効果ではない。

窓中心選択のoracle対称余裕rhoは1.9167→2.7917。同時変更のrho箱内部4,096点は
TP 3,815・FP 0・FN 88・TN 193、全512頂点はTP 338・FP 0・FN 102・TN 72。
断面から作った箱の全点が合格するという主張はしていない。

## 既知の制限

広域の独立評価seed 20260930・8,192条件では、再検証でもTP 80・FP 2・FN 406・TN 7,704だった。
支持度が高い条件でも誤許容があり、全域での誤許容0は未達である。
これは今回の3件の実装不具合と区別し、モデルの適用精度の制限として記録する。
自動出荷判定や実機の安全保証としてFIXしたものではない。

追加学習、異なる原料・モデルseedでの反復、境界の精密化、校正済み確率伝播は後続課題とする。

## 成果物

- [校正・方式比較](multistage_validation/results/calibration_fix_run.json)
- [一変数窓の再検証](multistage_validation/results/connected_window_validation.json)
- [中心選択・同時変更の再検証](multistage_validation/results/window_center_joint_validation.json)
- [広域の既知制限](multistage_validation/results/fix_broad_domain_limitation.json)
- [固定時のソースハッシュ](multistage_validation/results/v004_r001_fix_manifest.json)

Gitの既存の未コミット変更はまとめてコミットしていない。固定時点のソースは上記ハッシュで識別する。
