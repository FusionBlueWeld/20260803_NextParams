# 多工程シミュレータの選定と設計

## 参照した過去資料

指定された `20260623/_ml/_dataset/_generation` に相当する実在フォルダは
`C:/Users/tsuts/Documents/Codex/20260623_ml_dataset_generation` でした。
READMEの30項目と `src/dataset_generators.py`、関係するABOUT.mdを参照しました。
これらは設計資料であり、新しいコードの実行時依存ではありません。

|新しい工程|過去資料との対応|採用・拡張理由|
|---|---|---|
|レーザー溶接|既存溶接モデル、19レーザー加熱|これまでの比較結果を継続する基準|
|フライス切削|18切削主軸電流、08減衰振動|負荷・動力を品質と結び、回転数の共振を試す|
|プレス打抜き|17荷重波形、29プレス摩耗・材料ばらつき|バリ・荷重・平面度から適正条件を探す|
|熱硬化|07一次遅れ温度応答を反応工程へ拡張|時間・温度・層厚の連成と過処理を試す|
|熱風乾燥|07温度、12流量、28熱流体連成を乾燥工程へ拡張|水分移動とエネルギーの制約付き最小化|
|銅電解めっき|01電気、12流量を電気化学工程へ拡張|異分野の電荷と膜厚の関係、目標値への一致|

自由落下や単純ばね等は単体の回帰教材には有効ですが、加工結果の品質制約が
少なく、今回の工程最適化の比較対象としては優先順位を下げました。
設備故障ラベル、材料ロット、既に進んだ工具摩耗のような非操作量も、今回は
最適化の入力ノブにせず固定条件としています。環境差・経時変化は将来の拡張です。

熱硬化・乾燥・めっきは過去30項目にそのまま存在するモデルのコピーではなく、
そこで扱った温度・流体・電気の構造から、加工と品質の関係へ広げた新規モデルです。

## 比較で重視する差

レーザーだけでは最大化と離散品質制約への適応に偏ります。追加5工程によって、
最小化、内部最適、目標値近傍の谷、上限制約と下限制約の同時成立、
周期的な共振ピーク、時間累積、物質移動の飽和を検証対象に加えました。
同じ回帰器が工程を変えても、入力・出力の数、単位、スケール、方向に依存した
実装上の思い込みなく動くかを確認できます。

全候補を評価可能な規模に抑え、各工程のグリッド最良値を把握します。
その正解値は評価専用で、最適化への学習データは初期条件と実行済み条件のみです。
全グリッドへの予測精度に加え、未観測点に限定した予測精度も計算します。
各工程の候補数は同一ではありません。正規化regretは尺度を揃えますが、工程の
難易度まで等しくする指標ではないため、まず工程内で同じ予算を比較します。

## 高次元負荷として選んだ2工程

2026-09-05の追加改修では、フライス切削と銅電解めっきを3入力から8入力へ拡張した。
切削は材料除去率と動力の寸法関係、めっきはFaraday則による析出量という、
入力と出力の因果を追跡しやすい式が中核にあるため選定した。

フライスでは半径方向切込み、工具径、刃数、ノーズ半径、工具摩耗を入力化した。
めっきでは銅濃度、撹拌速度、電極間隔、デューティ比、浴pHを入力化した。
各軸4水準の直積は65,536点で、optimizer上限200,000点以内に保ちながら、
人が8変数の相互作用を同時に判断するには負荷の高い探索空間にした。

初期設計は全組合せではなく64点の決定論的maximin設計とする。
全候補の正解も計算可能なので、正解をoptimizerへ見せずに、探索後の正規化regretを
厳密に測れる。式の物理的骨格は明示できるが、切削力・粗さ・物質移動・効率の
補正式は検証用であり、実機校正済みのデジタルツインではない。

## 物理則と仮定した品質関数

各シミュレータは低次元の合成モデルです。以下の物理的骨格を使い、
品質・欠陥の補正式と係数はテスト用に設定しました。局所的な流れ場・応力場・
実物の検査画像までは出力しません。式、単位、固定条件は各工程READMEにあります。

- 切削：送り・切込み・回転数からの除去率と動力の換算を使用します。
  粗さに加える強制振動応答は、再生びびりの安定限界の完全解ではありません。
  [Sandvik Coromantの切削計算式](https://cdn.sandvik.coromant.com/files/sitecollectiondocuments/services/metal-cutting-e-learning/formulas-and-definitions/formulas-and-deinitions-for-milling-metric-enu.pdf)
- 打抜き：周長・板厚・せん断強さによる荷重と、クリアランスに伴う品質変化を
  組み合わせます。バリの補正式はこのベンチマーク独自です。
  [MISUMIのクリアランス解説](https://us.misumi-ec.com/blog/understanding-punch-and-die-clearance/)
- 熱硬化：一次遅れの加熱とArrhenius型の反応速度を積分して硬化率を求めます。
  劣化と接着強度への換算は仮定です。
  [COMSOLの熱硬化モデル解説](https://www.comsol.com/blogs/modeling-the-thermal-curing-process/)
- 乾燥：内部拡散・外部移動の抵抗と加熱・蒸発のエネルギーを組み合わせます。
  表面と内部の差を用いた欠陥指数は合成指標です。
  [NPTELの乾燥工学講義](https://archive.nptel.ac.in/content/storage2/courses/103103027/module4/lec1/2.html)
- めっき：Faraday則から電気量を析出膜厚へ換算します。効率・粗さに輸送限界を
  反映する補正を加えています。
  [COMSOLの電析モデル概要](https://doc.comsol.com/6.4/doc/com.comsol.help.edecm/edecm_introduction.02.01.html)

## 実装の分担

ユーザー指定どおり、新規5工程はそれぞれGPT-5.6-Lunaの別エージェントへ
工程フォルダ単位で割り当てました。共通APIの契約を先に渡し、親側で
レーザーの整理、共通実行器、バージョン接続、ノイズ処理、比較指標を実装しました。
返却されたモデルをレビューし、工程定義CSVの不整合やテストの読込問題は
担当へ差し戻したうえで、6工程共通の契約テストと実際のCLI接続で検証しています。

## 今後の拡張点

波形、材料ロットによる条件差、工具摩耗の進行、入力量の誤差、相関した検査誤差、
多目的最適化は今回の範囲には含めません。現在は単一目的・複数品質制約に揃え、
各工程の追加をフォルダと問題定義の追加で行えるようにしました。
物理モデルの改訂時はmanifestの版を更新し、監査結果と基準比較を再生成します。


## 2026-09-07時点の設計評価（履歴）

以下は当時の実装との差分・検討事項です。「未実装」「今後」は当時の状態を表します。
現在の入口と対応範囲は[README](README.md)を参照してください。

## v004・r001構想への適合評価と修正方針（2026-09-07）

**既存6工程はv004のv003互換経路を検証する正解関数として継続利用できます。**
一方、6工程をそのまま直列接続してr001の成立を検証する用途には適合しません。
工程接続の主試験は[複合工程側の評価・修正方針](../multistage/README.md)を使います。
以下はREADME・コードに基づく設計評価と今後の実装要件で、対応済みを意味しません。

### 形式・仕様の差と対応

| 現状・根拠 | 合わない理由 | 修正方針 |
|---|---|---|
| `simulators.py`は全入力を`parameter`としてグリッド化する | 操作条件と与えられる流入状態を区別できない | 既存CSVと標準挙動を維持し、接続試験だけ別設定・アダプターで役割を分離する。流入状態を推薦対象にしない |
| 学習CSVは`problem.csv`掲載出力のみ | 物理モデルが返す診断値がすべて接続用の学習対象になるわけではない | 接続試験では必要出力をmonitor等で明示する。既存出力の意味・列順を変えない |
| `benchmark.versions()`は実装CLIを検出し、実行は共通`main.py`経由 | v004は未実装で、共通入口にも未登録 | v004実装時に入口を登録し、既存6工程・同じseed・同じ予算で互換経路を比較する |
| 既存suiteは既定の知識・領域・停止設定による固定予算比較 | 現場の非既定設定や推奨停止の操作互換まで保証しない | 知識、禁止・優先領域、必須／推奨停止、反復、monitorを含む専用互換ケースを追加する |
| 全候補はparameterの直積で上限200,000 | 流入状態をそのまま探索軸へ追加すると規模と役割が変わる | 操作候補と流入シナリオを分離し、一括予測を分割実行する。旧グリッドは回帰基準として固定する |
| `measure()`のノイズは出力ごとの独立加法正規ノイズ | 検査ノイズであり、工程ばらつき・共通ロット変動・モデル不確実性を表さない | 現行ノイズは測定互換試験に残す。確率接続用は別の生成層に要因・相関・seed・真値／測定値を記録する |

v004の互換経路では、入力・設定を変更せず、推薦条件・順位・停止状態・CSV契約をv003と
照合します。同じ環境とseedを使い、数値許容差を定義します。既存oracleの係数、閾値、
グリッドをv004に都合よく変更しません。拡張ケースは別の版・設定・結果として保存します。

### 工程別の物理指標の適否

以下の指標は現在の局所目的・制約の検証には使用できます。ただし、下流が必要とする
材料状態と同義ではありません。単位が一致しても材料、測定基準、工程履歴の意味を確認します。

| 工程 | 接続検証での注意点と修正方針 |
|---|---|
| laser_welding | 溶込み深さと0〜9のスパッタレベルは局所品質。下流状態への対応式は未定義。接続するなら形状・材料・熱履歴等の必要状態を別途定義する。スパッタの順序尺度を連続量や故障確率に読み替えない |
| milling | 除去率・粗さ・動力は局所評価に適するが、加工後形状や残留状態を網羅しない。`tool_wear_mm`は現在parameterだが、摩耗を外部状態とする接続試験では任意設定で固定・供給する。工具選択と摩耗を自由な連続操作として混同しない |
| press_forming | バリ高さ・荷重・平面度は使用可能。上流板厚・材質などの流入状態を受け取る契約はなく、同じ名前の品質量を受け渡すだけでは工程接続にならない。対象ラインが決まった段階で入力と応答式を拡張する |
| thermal_curing | 層厚はmm、複合工程側の乾燥膜厚はum。換算だけでは残留溶剤・応力・欠陥の影響を補えない。旧モデルは互換検証用に残し、複合工程のcuringを接続用に使用する。層厚を流入状態とする役割分離試験は別設定にする |
| convection_drying | 固定した水系膜の残留水分率（湿量基準）であり、複合工程の可変膜厚・残留溶剤率とは対象が異なる。列名変更で代用せず、複合工程のdryingを使用する |
| electroplating | `thickness_error_um`は目的用の膜厚誤差で、下流へ渡す絶対膜厚ではない。既存monitorの`deposit_thickness_um`を接続候補にする。粗さ・膜厚だけで下流に十分かは別途定義し、浴状態の操作可能性も案件ごとに指定する |

これらの修正候補は、6工程すべてをr001用に作り替える要求ではありません。まず既存6工程で
操作互換を確認し、接続の学習・分布検証は専用の塗工→乾燥→熱硬化で進めます。

### 評価指標と追加する検証

- 既存のregret、MAE/RMSE、制約適合率、Brier scoreは単工程比較に維持する。
- 現行Brier scoreは決定論oracleの合否との比較であり、工程ばらつきに対する製品良品率の
  校正試験ではない。確率接続では反復シナリオの合否と照合する評価を別に追加する。
- 流入状態を変える拡張試験では、操作条件と流入状態をともに変えた学習設計を用意する。
  固定流入状態での学習から、任意状態へ使えると判断しない。
- 製品・ロット・反復の識別情報を別途保持し、同じ製品やロットが学習・検証へ混入しない
  分割を追加する。oracle真値・全グリッド情報は採点側に限定する。

物理式を変更する場合はモデル版・ハッシュ・基準結果を更新し、旧回帰基準を保存します。
この評価は内部ベンチマークとしての適合性であり、採用指標の実機校正を行ったものではありません。

